# Berkshire Hathaway 13F Tracker

An MVP dashboard for tracking Berkshire Hathaway Inc. 13F-HR holdings from SEC EDGAR. The app is static: Vite, React, TypeScript, Tailwind CSS, and JSON files generated by Python scripts.

CIK: `0001067983`

## Local Dev

```bash
npm install
npm run dev
```

## Fetch Data

The fetch script uses SEC public endpoints only and does not require an API key.

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python scripts/fetch_13f.py
```

Generated files:

- `public/data/latest.json`
- `public/data/history.json`
- `public/data/changes.json`
- `public/data/quarters.json`

## Build

```bash
npm run build
```

The production output is written to `dist/`.

## Deploy to GitHub Pages

This repository includes `.github/workflows/deploy-pages.yml`.

In GitHub:

1. Open repository settings.
2. Go to Pages.
3. Set source to GitHub Actions.
4. Push to `main` or run the `Deploy GitHub Pages` workflow manually.

The Vite `base` path is configured automatically from `GITHUB_REPOSITORY` during GitHub Actions builds.

## GitHub Actions

`Update 13F Data` runs every Monday and can also be started manually. It:

1. Installs Python dependencies.
2. Fetches the latest two Berkshire Hathaway 13F-HR filings from SEC EDGAR.
3. Regenerates JSON data in `public/data/`.
4. Commits changed JSON files back to the repository.

`Deploy GitHub Pages` builds the React app and publishes `dist/` to GitHub Pages.

## Data Notes

- SEC information table `value` fields are written to JSON as reported by the XML source.
- Tickers are filled from a local CUSIP mapping where available and remain `null` when no reliable mapping exists.
- Quarterly change actions are based on share count changes by CUSIP:
  - `New Position`
  - `Added`
  - `Reduced`
  - `Unchanged`
  - `Sold Out`
